

part of '../onboard_view.dart';

class _OnboardHover extends StatelessWidget {
  const _OnboardHover({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
                  width: 10,
                  height: 10,
                  decoration: BoxDecoration(shape:BoxShape.circle,color: Colors.red,),
                );
  }
}